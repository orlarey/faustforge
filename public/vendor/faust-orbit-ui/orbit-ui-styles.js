// AUTO-GENERATED — do not edit. Regenerated by scripts/build-styles.mjs.
// Concatenation of src/faust-orbit-ui.css and src/orbit-calque.css,
// inlined so the OrbitUI wrapper can inject it inside its shadow root
// without depending on the host loading a stylesheet link.
export const ORBIT_UI_STYLES = `/* ============================================================
   Public theming surface — CSS custom properties exposed to hosts.
   Hosts override these on the shadow-host element (the \`<div>\` they
   pass to \`new OrbitUI(container, …)\`). Custom properties pierce the
   shadow boundary, so a host's \`.my-orbit { --orbit-pill-bg: … }\`
   reaches every styled element inside the shadow.
   ============================================================ */
/* The shadow host needs an explicit display + size so the chain of
   \`height: 100%\` rules below resolves against a definite box, instead
   of collapsing the orbit to a content-sized strip. Hosts can still
   override on the outside (e.g. \`position: absolute\`, fixed pixel
   sizes) — \`:host\` has the lowest specificity in the cascade. */
:host {
  display: block;
  width: 100%;
  height: 100%;

  /* Backgrounds */
  --orbit-bg: #0d1016;            /* canvas / main body */
  --orbit-toolbar-bg: #141b25;    /* top toolbar + bottom detail panel */
  --orbit-toolbar-line: #263242;  /* divider lines, slider track */

  /* Pills (Center / Random / Zoom / Library / Trash / preset value box) */
  --orbit-pill-bg: #1a2230;
  --orbit-pill-border: #3a4a5f;
  --orbit-pill-hover-bg: #25324a;

  /* Foreground / accent */
  --orbit-fg: #d7dde7;            /* pill text, slider thumb, value labels */
  --orbit-fg-muted: #b9ccdf;
  --orbit-accent-bg: #2b4d6e;     /* active / pressed pill */
  --orbit-accent-fg: #eaf2fb;
  --orbit-accent-border: #4a7298;
}

/* Inner shadow-host div: fills the host, so the .orbit-wrap grid below
   can resolve its \`height: 100%\` against a definite box. */
.orbit-shadow-host {
  display: block;
  width: 100%;
  height: 100%;
}

/* Re-declare the Material Symbols Outlined class-to-font mapping inside
   the shadow tree. Google Fonts' stylesheet declares this at document
   level — its rules don't pierce shadow boundaries, even though the
   \`@font-face\` itself (loaded by the host page) is globally available. */
.material-symbols-outlined {
  font-family: 'Material Symbols Outlined', sans-serif;
  font-weight: normal;
  font-style: normal;
  line-height: 1;
  letter-spacing: normal;
  text-transform: none;
  display: inline-block;
  white-space: nowrap;
  word-wrap: normal;
  direction: ltr;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  text-rendering: optimizeLegibility;
  font-feature-settings: 'liga';
}

.orbit-wrap {
  display: grid;
  grid-template-rows: auto 1fr auto;
  height: 100%;
  background: var(--orbit-bg);
  /* Positioning context for the calque's portamento bar (which is
     appended here so FaustOrbitUI's innerHTML reset on .orbit-detail
     doesn't wipe it) AND container-query host so the bar's responsive
     layout can match \`@container orbit-detail (max-width: …)\`. */
  position: relative;
  container-type: inline-size;
  container-name: orbit-detail;
}

.orbit-header {
  position: relative;
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 6px 10px;
  border-bottom: 1px solid var(--orbit-toolbar-line);
  background: var(--orbit-toolbar-bg);
  /* Container query host so the toolbar can collapse to icon-only at
     narrow widths regardless of viewport size. */
  container-type: inline-size;
  container-name: orbit-header;
}

/* Push the zoom group to the right edge so the toolbar reads as
   \`[host-injected presets actions] [center / random] ... [zoom]\`. */
.orbit-zoom-wrap {
  margin-left: auto;
}

.orbit-middle-actions,
.orbit-zoom-wrap {
  display: flex;
  align-items: center;
  gap: 8px;
}

.orbit-random-group,
.orbit-zoom-group {
  display: inline-flex;
  align-items: center;
  position: relative;
  border: 1px solid var(--orbit-pill-border);
  border-radius: 999px;
  overflow: hidden;
}

.orbit-zoom-label {
  padding: 4px 8px;
  font-size: 0.6rem;
  color: var(--orbit-fg);
}

.orbit-center-btn,
.orbit-random-btn,
.orbit-random-mix,
.orbit-zoom {
  border: 1px solid var(--orbit-pill-border);
  background: var(--orbit-pill-bg);
  color: var(--orbit-fg);
  font: inherit;
  font-size: 0.6rem;
  line-height: 1;
}

.orbit-center-btn,
.orbit-random-btn {
  border-radius: 999px;
  padding: 4px 10px;
}

.orbit-random-mix,
.orbit-zoom {
  border: 0;
  border-left: 1px solid var(--orbit-pill-border);
  padding: 4px 8px;
}

.orbit-random-btn {
  border: 0;
  padding: 4px 10px;
}

/* Material Symbols icons inline inside the orbit toolbar buttons / label.
   Sized slightly larger than the surrounding 0.6rem text so the glyph
   shape stays readable. The font itself must be loaded by the host page
   (e.g. Google Fonts: Material Symbols Outlined). */
.orbit-center-btn .material-symbols-outlined,
.orbit-random-btn .material-symbols-outlined,
.orbit-zoom-label.material-symbols-outlined {
  font-size: 13px;
  line-height: 1;
  vertical-align: middle;
  font-variation-settings: "FILL" 0, "wght" 500, "GRAD" 0, "opsz" 20;
}

/* Narrow toolbar tier: collapse Random and Zoom to icon-only. The
   selects stay in DOM (so showPicker() can still target them) but lose
   their visible track and pointer hits. The icons are wrapped in a
   click+long-press handler at the OrbitUI level: short tap = action,
   long press = native picker. The group's pill border stays so the
   icon still reads as a pill control, consistent with the other
   toolbar items. */
@container orbit-header (max-width: 480px) {
  .orbit-random-mix,
  .orbit-zoom {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: 0;
    border: 0;
    overflow: hidden;
    opacity: 0;
    pointer-events: none;
  }
  .orbit-random-btn,
  .orbit-zoom-label {
    cursor: pointer;
  }
}

.orbit-body {
  position: relative;
  min-height: 0;
  overflow: hidden;
}

.orbit-canvas {
  display: block;
  width: 100%;
  height: 100%;
}

/* ── Detail panel (bottom bar for last-touched control) ── */

.orbit-detail {
  display: grid;
  grid-template-columns: auto 1fr;
  align-items: center;
  gap: 10px;
  height: 48px;
  padding: 0 12px;
  border-top: 1px solid var(--orbit-toolbar-line);
  background: var(--orbit-toolbar-bg);
  overflow: hidden;
  /* Container query host — the calque's portamento bar adapts its
     layout to the detail bar's actual width (regardless of viewport).
     The bar itself is now a sibling of .orbit-detail (under .orbit-wrap),
     so the container-name has to live on .orbit-wrap as well. We keep
     it here too in case future rules want to query the detail row's
     own width. */
  container-type: inline-size;
  container-name: orbit-detail;
}


.orbit-detail-empty {
  grid-column: 1 / -1;
  font-size: 12px;
  color: #556575;
  text-align: center;
  user-select: none;
}

.orbit-detail-label {
  display: flex;
  align-items: center;
  gap: 6px;
  min-width: 0;
  white-space: nowrap;
  overflow: hidden;
}

.orbit-detail-color {
  flex-shrink: 0;
  width: 8px;
  height: 8px;
  border-radius: 50%;
}

.orbit-detail-name {
  font-size: 12px;
  font-weight: 600;
  color: #b9ccdf;
  overflow: hidden;
  text-overflow: ellipsis;
}

.orbit-detail-slider-wrap {
  display: grid;
  grid-template-columns: auto 1fr auto;
  align-items: center;
  gap: 8px;
}

.orbit-detail-value {
  width: 72px;
  padding: 3px 6px;
  border: 1px solid var(--orbit-pill-border);
  border-radius: 4px;
  background: var(--orbit-pill-bg);
  color: var(--orbit-fg);
  font-size: 12px;
  font-family: inherit;
  text-align: right;
  outline: none;
}

.orbit-detail-value:focus {
  border-color: #5a8abf;
}

.orbit-detail-range {
  -webkit-appearance: none;
  appearance: none;
  width: 100%;
  height: 4px;
  /* Padding / border explicit to 0 so a host's generic
     \`input { padding: …; border: … }\` rule cannot bleed through and
     turn the 4px-tall track into a thick rectangular box. */
  padding: 0;
  border: 0;
  border-radius: 2px;
  background: var(--orbit-toolbar-line);
  outline: none;
  cursor: pointer;
}

.orbit-detail-range::-webkit-slider-thumb {
  -webkit-appearance: none;
  width: 14px;
  height: 14px;
  border-radius: 50%;
  background: var(--orbit-fg);
  border: 2px solid var(--orbit-bg);
  cursor: pointer;
}

.orbit-detail-range::-moz-range-thumb {
  width: 14px;
  height: 14px;
  border-radius: 50%;
  background: var(--orbit-fg);
  border: 2px solid var(--orbit-bg);
  cursor: pointer;
}

.orbit-detail-bound {
  font-size: 11px;
  color: #556575;
  white-space: nowrap;
  user-select: none;
}

.orbit-detail:has(.orbit-detail-button-wrap) .orbit-detail-label {
  grid-row: 1;
  grid-column: 1 / -1;
}

.orbit-detail-button-wrap {
  grid-row: 1;
  grid-column: 1 / -1;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
}

.orbit-detail-btn {
  padding: 5px 16px;
  border: 1px solid var(--orbit-pill-border);
  border-radius: 4px;
  background: var(--orbit-pill-bg);
  color: var(--orbit-fg);
  font-size: 12px;
  font-family: inherit;
  cursor: pointer;
  user-select: none;
}

.orbit-detail-btn:active,
.orbit-detail-btn.orbit-detail-btn--active {
  background: #2a3a50;
  border-color: #5a8abf;
}

.orbit-detail-checkbox {
  width: 16px;
  height: 16px;
  accent-color: #5a8abf;
  cursor: pointer;
}

.orbit-detail-select {
  padding: 5px 10px;
  border: 1px solid var(--orbit-pill-border);
  border-radius: 4px;
  background: var(--orbit-pill-bg);
  color: var(--orbit-fg);
  font-size: 12px;
  font-family: inherit;
  cursor: pointer;
  outline: none;
}

.orbit-detail-select:focus {
  border-color: #5a8abf;
}

/* Library toggle button injected into the orbit-ui header. Icon-only,
   styled to blend with .orbit-center-btn / .orbit-random-btn. Pinned
   to the geometric centre of .orbit-header so it stays put regardless
   of the side groups' widths. */
.orbit-library-btn {
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  border: 1px solid var(--orbit-pill-border);
  border-radius: 999px;
  padding: 4px 10px;
  background: var(--orbit-pill-bg);
  color: var(--orbit-fg);
  font: inherit;
  font-size: 0.6rem;
  line-height: 1;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
}

.orbit-library-btn .material-symbols-outlined {
  font-size: 13px;
  line-height: 1;
  vertical-align: middle;
  font-variation-settings: "FILL" 0, "wght" 500, "GRAD" 0, "opsz" 20;
}

/* Re-assert the centering transform on every state of the button so a
   host's generic \`button:hover { transform: ... }\` (e.g. webdaw's
   "lift on hover" effect) cannot win the cascade and yank the button
   off-centre. The transform here is identical to the base rule. */
.orbit-library-btn:hover,
.orbit-library-btn:focus,
.orbit-library-btn:active,
.orbit-library-btn[aria-pressed='true'] {
  transform: translate(-50%, -50%);
}

.orbit-library-btn:hover {
  background: var(--orbit-pill-hover-bg);
}

.orbit-library-btn[aria-pressed='true'] {
  background: var(--orbit-accent-bg);
  color: var(--orbit-accent-fg);
  border-color: var(--orbit-accent-border);
}

/* Niveau-1 calque overlay rendered above the FaustOrbitUI canvas. */
.orbit-ui-overlay {
  position: absolute;
  inset: 0;
  pointer-events: auto;
  user-select: none;
  -webkit-user-select: none;
}

.orbit-ui-overlay-canvas {
  display: block;
  width: 100%;
  height: 100%;
  cursor: crosshair;
}

.orbit-ui-overlay:focus {
  outline: none;
}

.orbit-ui-overlay.orbit-ui-overlay-active {
  outline: 1px solid rgba(122, 215, 255, 0.25);
  outline-offset: -1px;
}

/* Trash button injected into the orbit-ui toolbar. Same pill styling
   as Center / Library / Random group so the toolbar reads
   consistently. Hidden when the selection is empty so the layout
   reads cleanly when there's nothing to delete. */
.orbit-trash-btn {
  border: 1px solid var(--orbit-pill-border);
  border-radius: 999px;
  padding: 4px 10px;
  background: var(--orbit-pill-bg);
  color: var(--orbit-fg);
  font: inherit;
  font-size: 0.6rem;
  line-height: 1;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
}

.orbit-trash-btn[hidden] {
  display: none !important;
}

.orbit-trash-btn .material-symbols-outlined {
  font-size: 13px;
  line-height: 1;
  vertical-align: middle;
  font-variation-settings: "FILL" 0, "wght" 500, "GRAD" 0, "opsz" 20;
}

.orbit-trash-btn:hover {
  background: rgba(60, 30, 30, 0.85);
  color: #ffd0d0;
}

/* Preset badge — pill-shaped group mirroring the Random / Zoom groups:
   \`[label-icon] | [display]\` with a vertical divider. The display
   shows the active preset's name when the current params match a
   named preset, otherwise the count (or \`K/N\` when there's a
   selection). A transparent <select> overlays the whole group as the
   state holder. */
.orbit-presets-group {
  position: relative;
  display: inline-flex;
  align-items: center;
  border: 1px solid var(--orbit-pill-border);
  border-radius: 999px;
  background: var(--orbit-pill-bg);
  overflow: hidden;
  cursor: pointer;
  user-select: none;
}

.orbit-presets-group:hover {
  background: var(--orbit-pill-hover-bg);
}

.orbit-presets-icon {
  padding: 4px 8px;
  font-size: 0.6rem;
  line-height: 1;
  color: var(--orbit-fg);
  pointer-events: none;
}

.orbit-presets-icon.material-symbols-outlined {
  font-size: 13px;
  vertical-align: middle;
  font-variation-settings: "FILL" 0, "wght" 500, "GRAD" 0, "opsz" 20;
}

.orbit-presets-display {
  border-left: 1px solid var(--orbit-pill-border);
  padding: 4px 10px;
  min-width: 30px;
  box-sizing: border-box;
  font: inherit;
  font-size: 0.6rem;
  line-height: 1;
  color: var(--orbit-fg);
  font-variant-numeric: tabular-nums;
  white-space: nowrap;
  text-align: center;
  pointer-events: none;
}

/* Unified custom dropdown — used by Random, Zoom, and the Presets
   menu (replacing the native <select> popups so we can theme them).
   Anchored under the trigger via JS, sits in document.body. */
.orbit-dropdown-menu {
  position: fixed;
  z-index: 10000;
  min-width: 100px;
  max-height: 60vh;
  overflow-y: auto;
  padding: 4px;
  border: 1px solid var(--orbit-pill-border);
  border-radius: 6px;
  background: var(--orbit-toolbar-bg);
  box-shadow: 0 6px 20px rgba(0, 0, 0, 0.55);
  font: inherit;
  font-size: 0.6rem;
  line-height: 1;
  color: var(--orbit-fg);
}

.orbit-dropdown-menu-item {
  display: block;
  width: 100%;
  /* Reserve space for the active ✓ in the gutter so all items align. */
  padding: 5px 8px 5px 20px;
  border: 0;
  border-radius: 4px;
  background: transparent;
  color: inherit;
  font: inherit;
  text-align: left;
  cursor: pointer;
  position: relative;
  white-space: nowrap;
}

.orbit-dropdown-menu-item:hover:not(:disabled) {
  background: rgba(232, 201, 122, 0.18);
  color: rgb(255, 230, 170);
}

.orbit-dropdown-menu-item:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.orbit-dropdown-menu-item--active {
  color: rgb(255, 230, 170);
}

.orbit-dropdown-menu-item--active::before {
  content: "✓";
  position: absolute;
  left: 6px;
  top: 50%;
  transform: translateY(-50%);
  color: rgb(232, 201, 122);
  font-weight: 600;
}

.orbit-dropdown-menu-separator {
  height: 1px;
  margin: 4px 4px;
  background: #2e3642;
}

.orbit-presets-select {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  margin: 0;
  padding: 0;
  border: 0;
  background: transparent;
  color: transparent;
  /* Match the typography of the other native selects (.orbit-zoom,
     .orbit-random-mix) so the OS dropdown renders options at the same
     size as theirs. \`font: inherit\` alone wasn't enough on every browser
     — the shorthand can clobber font-size. Set it explicitly. */
  font: inherit;
  font-size: 0.6rem;
  line-height: 1;
  cursor: pointer;
  opacity: 0;
}

/* Style the dropdown options to match the toolbar background. Chromium
   honors these on <option>; Safari ignores them and falls back to the
   system style — there's no portable CSS to fully restyle a native
   <select> popup. */
.orbit-presets-select option {
  background-color: #141B25;
  color: var(--orbit-fg);
}

.orbit-presets-select option:disabled {
  color: #5a6678;
}

.orbit-ui-overlay-name-input {
  position: absolute;
  z-index: 2;
  border: 1px solid rgba(232, 197, 98, 0.7);
  border-radius: 4px;
  padding: 3px 6px;
  background: rgba(20, 27, 37, 0.95);
  color: #f4e8c8;
  font-size: 12px;
  line-height: 1.2;
  outline: none;
}

.orbit-ui-overlay-name-input::placeholder {
  color: rgba(244, 232, 200, 0.45);
}

.orbit-ui-overlay-name-input:focus {
  border-color: #e8c562;
  box-shadow: 0 0 0 2px rgba(232, 197, 98, 0.25);
}

/* Portamento bar — overlays the orbit-ui's own detail bar (the bottom
   slider area) while the calque is open. Anchored to the bottom of
   .orbit-wrap so FaustOrbitUI's innerHTML resets on .orbit-detail (when
   a parameter is selected for detail editing) don't wipe it. Same 48 px
   height as the detail row beneath. Opaque backdrop hides the host's
   detail content while shown. */
.orbit-ui-overlay-portamento {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  height: 48px;
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 0 12px;
  background: rgba(20, 27, 37, 0.96);
  border-top: 1px solid var(--orbit-toolbar-line);
  z-index: 4;
  color: var(--orbit-fg);
  font: inherit;
  font-size: 0.6rem;
  line-height: 1;
  box-sizing: border-box;
}

/* Compound selector (specificity 0,2,0) so it beats Google Fonts'
   \`.material-symbols-outlined { font-size: 24px }\` rule (0,1,0) which
   loads from the CDN AFTER our stylesheet and would otherwise win the
   tie. The top-toolbar icons (Center / Random / Zoom / Library /
   Trash / Presets) survive for the same reason — their selectors are
   already compound. */
.orbit-ui-overlay-portamento-icon.material-symbols-outlined,
.orbit-ui-overlay-portamento-icon {
  font-size: 13px;
  line-height: 1;
  font-variation-settings: "FILL" 0, "wght" 500, "GRAD" 0, "opsz" 20;
  color: var(--orbit-fg-muted);
  /* Non-interactive labels — disable text-cursor on hover and prevent
     accidental selection of the underlying glyph name. */
  cursor: default;
  user-select: none;
}

/* Slot wraps a slider + its value label so the two stay together
   inside one flex item. Each slot gets flex:1, so s1 and s2 always
   share the available space evenly. */
.orbit-ui-overlay-slot {
  flex: 1;
  display: flex;
  align-items: center;
  gap: 8px;
  min-width: 0;
}

.orbit-ui-overlay-portamento input[type='range'] {
  flex: 1;
  -webkit-appearance: none;
  appearance: none;
  height: 4px;
  /* Padding / border explicit to 0 so a host's generic
     \`input { padding: …; border: … }\` rule cannot bleed through. */
  padding: 0;
  border: 0;
  border-radius: 2px;
  background: var(--orbit-toolbar-line);
  outline: none;
  cursor: pointer;
  margin: 0;
}

.orbit-ui-overlay-portamento input[type='range']::-webkit-slider-thumb {
  -webkit-appearance: none;
  width: 12px;
  height: 12px;
  border-radius: 50%;
  background: var(--orbit-fg);
  border: 2px solid var(--orbit-bg);
  cursor: pointer;
}

.orbit-ui-overlay-portamento input[type='range']::-moz-range-thumb {
  width: 12px;
  height: 12px;
  border-radius: 50%;
  background: var(--orbit-fg);
  border: 2px solid var(--orbit-bg);
  cursor: pointer;
}

.orbit-ui-overlay-portamento-value {
  min-width: 56px;
  text-align: right;
  font-variant-numeric: tabular-nums;
  color: var(--orbit-fg-muted);
  white-space: nowrap;
}

/* Each slider keeps a usable minimum so it never collapses to nothing. */
.orbit-ui-overlay-portamento input[type='range'] {
  min-width: 40px;
}

/* Tier 1: shed the inline value labels — the slider thumbs already
   encode the value. Reveal them again while the user is dragging or
   has focused the matching slider (\`is-active\` driven by JS), as a
   small bubble centred above the thumb so it doesn't need horizontal
   room. With the labels gone, the sliders' flex:1 takes over the slot
   and they grow to fill it. */
@container orbit-detail (max-width: 480px) {
  .orbit-ui-overlay-portamento-value {
    display: none;
  }
  .orbit-ui-overlay-portamento-value.is-active {
    display: inline-block;
    position: absolute;
    top: 0;
    /* \`left\` is set inline by JS to follow the thumb. */
    transform: translateX(-50%);
    min-width: 0;
    padding: 1px 6px;
    border: 1px solid rgba(122, 215, 255, 0.45);
    border-radius: 4px;
    background: rgba(20, 27, 37, 0.96);
    color: var(--orbit-accent-fg);
    pointer-events: none;
    z-index: 5;
    box-shadow: 0 1px 4px rgba(0, 0, 0, 0.45);
  }
}

/* Mirror the \`.orbit-center-btn\` / \`.orbit-random-btn\` pill so the
   loop play/stop button reads as part of the same toolbar family. */
.orbit-ui-overlay-loop-btn {
  border: 1px solid var(--orbit-pill-border);
  background: var(--orbit-pill-bg);
  color: var(--orbit-fg);
  font-size: 0.6rem;
  line-height: 1;
  padding: 4px 10px;
  border-radius: 999px;
  cursor: pointer;
}

/* Compound selector (specificity 0,2,0) so we beat Google Fonts'
   \`.material-symbols-outlined { font-size: 24px }\` rule (0,1,0).
   The button itself carries the Material class — its sole content is
   the play / stop glyph. */
.orbit-ui-overlay-loop-btn.material-symbols-outlined {
  font-size: 13px;
  font-variation-settings: "FILL" 0, "wght" 500, "GRAD" 0, "opsz" 20;
}

.orbit-ui-overlay-loop-btn:hover:not(:disabled) {
  background: var(--orbit-pill-hover-bg);
}

.orbit-ui-overlay-loop-btn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}
`;
//# sourceMappingURL=orbit-ui-styles.js.map